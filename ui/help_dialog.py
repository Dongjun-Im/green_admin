"""단축키/사용법 안내 대화상자."""
import wx

from config import APP_NAME, APP_VERSION
from ui.item_text_ctrl import ItemTextCtrl


SHORTCUTS_TEXT = """초록등대 회원관리 — 단축키 안내

[작업]
  Ctrl+F          회원 검색 (아이디·이름·닉네임 부분 일치)
  Ctrl+G          (검색 창에서) 선택한 회원 등급 변경 — 단건 승급/강등
  Ctrl+T          회원 통계 (등급별 인원 + 최근 30/90일 활동)
  Ctrl+B          우수회원 백업 즉시 실행 (TXT + Excel)
                  · 백업 후 게시물 기반 자동 승급도 함께 진행
  Ctrl+U          게시물 기반 자동 승급 단독 실행
                  · '우리들의 이야기' 게시판 글 수 기준
  Ctrl+R          장기미접속 조정 — 미리보기 (드라이런)
  Ctrl+Shift+R    장기미접속 조정 — 실제 적용 (확인 후)
  Ctrl+Z          마지막 작업 되돌리기 (자동 승급/조정/수동 변경 복구)
  Ctrl+M          수동 메일 발송 (rtgreen 전용)
                  · 자동 발송이 실패했을 때 다시 시도
                  · rtgreen 외 계정은 안내 후 종료
  Ctrl+D          지금 작업 가능 여부 확인 (음성)

[정보]
  Ctrl+I          마지막 작업일 / 다음 예정일 음성 안내
  Ctrl+K          이 단축키 안내
  F1              프로그램 정보

[파일]
  Ctrl+O          백업 폴더 열기
  Ctrl+Shift+Y    등급 변경 이력 (영구 SQLite 저장 — v1.0)
  Ctrl+Shift+D    백업 비교 (분기 간 신규/승급/강등/빠짐)
  Ctrl+Shift+L    작업 로그 뷰어 (월별 operation 로그 조회)
  Ctrl+L          로그아웃 (저장된 자격 증명 삭제 후 재로그인)
  Alt+F4          프로그램 종료

[목록 탐색]
  ↑ / ↓           이전/다음 항목
  Home / End      처음/끝 항목
  Esc             현재 음성 중단

[v1.0 안정 버전 — 신규]
  · 수동 메일 발송에서 등급별 일괄 수신자 선택 (5~9 각 등급 / 가입 완료 전체).
        Ctrl+F 로 회원 목록을 한 번 캐시하면 라디오 옵션이 활성화됩니다.
        5개 등급은 0명이어도 항상 표시되어 분포를 한눈에 확인할 수 있어요.
  · 수동 메일 첨부파일 — 한 개 이상 파일을 모든 수신자에게 함께 발송.
        발송 화면 아래 "첨부파일" 영역에서 추가/제거/전체 제거.
  · 신규 가입 승인 시 환영 메일 자동 발송 (rtgreen 계정 로그인 시).
        승인 → 등급 변경 → 환영 메일 발송이 한 번에 진행됩니다.
  · 회원 등급 변경 영구 이력 — 모든 변경(자동 승급/조정/수동/가입 처리/되돌리기)이
        data/level_history.db 에 영구 보관. 파일 메뉴 → "등급 변경 이력
        (Ctrl+Shift+Y)" 으로 회원 ID·유형 필터 + 검색.
  · 사이트 구조 변경 자동 진단 — 회원 목록 분석 실패 시 사이트 응답을
        자동으로 분석해 구체적 원인 메시지 제공. 개발자 메뉴에 수동 호출도 추가.
  · 자동 업데이트 확인 — 시작 시 백그라운드(24h 캐시), 새 버전 릴리스가
        있으면 알림. 도움말 → "업데이트 확인" 으로 강제 호출 가능.
  · 단위 테스트 — pytest 60+ 케이스로 회귀 보호.

[v0.5 신규 워크플로]
  · 신규 가입자 승인 — 가입 신청·대기 등급(3,4) 회원이 있으면
        프로그램 시작 시 자동 알림. "지금 처리하시겠습니까?" 질문창 →
        한 명씩 승인/거부/미루기. 작업 메뉴에서 언제든 다시 불러올 수 있음.
  · 자동 승급 기준이 "글 수" 에서 "활동점수(글+댓글)" 로 확장되었습니다.
        활동점수 = 글수 × 1.0 + 댓글수 × 0.3
  · 산정 게시판: 우리들의 이야기(green3) + 질문게시판(green9)
  · 임계값:
        가입신청·대기 → 준회원(5):    활동점수 3 이상
        준회원(5)     → 일반회원(6):  활동점수 5 이상   ← v0.5 신규
        일반회원(6)   → 우수(7):      활동점수 30 이상
        일반회원(6)   → 최우수(8):    활동점수 60 이상
        일반회원(6)   → 명예(9):      활동점수 300 이상
  · 분기 MVP TOP 10 — 매년 1/4/7/10월 1일 도래.
        백업 자동 실행 후 이어서 자동 분석되거나, "MVP TOP 10 분석" 메뉴로
        언제든 수동 실행. 결과는 backups/mvp_top10_YYYY-Q.txt 로 저장.
  · 메일 발송 방식이 "개별 발송 한 가지" 로 통일.
        수신자에게 다른 수신자 ID 가 절대 노출되지 않습니다.

[v0.4 신규 워크플로]
  · 자동 승급도 미리보기 → 승인 → 적용의 2 단계로 변경.
    실수로 즉시 적용되는 일이 사라졌습니다.
  · 자동 승급 / 장기미접속 조정 / 수동 등급 변경 후
    Ctrl+Z 로 가장 최근 작업을 통째로 되돌릴 수 있습니다.
    되돌리기 기록은 최근 10개까지 data/undo_stack.json 에 보존.
  · "오래된 백업 정리" 메뉴로 12개월 이상 백업을 zip 으로 보관.
    zip 자체는 자동 삭제하지 않습니다.
  · 회원 검색에서 회원 선택 후 Ctrl+N 으로 메모/태그 편집.
    메모/태그가 있는 회원은 ★ 마커로 표시됩니다.
  · "승급 임박 회원 분석" 메뉴로 곧 우수/최우수/명예가 될 회원 확인.
  · "HTML 리포트 만들기" 메뉴로 분기 운영보고용 한 페이지 HTML 출력.
  · "단축키 설정 파일" 메뉴로 keybindings.json 을 열어 단축키 커스터마이징.
    수정 후 프로그램을 재시작하면 사용자 단축키가 추가 적용됩니다.

[규칙 - 사이트 실제 등급 기준 (v0.4 새 매핑)]
  등급 번호: 5 준회원 / 6 일반회원 / 7 우수회원 / 8 최우수회원 / 9 명예회원
  (동호회관리자 권한은 별도 — 사이트 페이지 접근권으로 판정)

  · 우수회원 백업: 7레벨(우수) + 8레벨(최우수), 3개월 주기
        매년 1/4/7/10월 1일 도래 (캘린더 기준)
  · 활동점수 기반 자동 승급 (v0.5 — green3 + green9 글·댓글 합산):
        - 대기/신청(3,4) + 활동점수 3 이상   → 준회원(5)
        - 준회원(5)   + 활동점수 5 이상   → 일반회원(6)
        - 일반회원(6) + 활동점수 30 이상  → 우수회원(7)
        - 일반회원(6) + 활동점수 60 이상  → 최우수회원(8)
        - 일반회원(6) + 활동점수 300 이상 → 명예회원(9)
        * 활동점수 = 글수 × 1.0 + 댓글수 × 0.3
        * 기존 등급보다 낮아지는 강등은 발생하지 않음
        * 3개월 주기 백업 시점에 함께 자동 실행
  · 자동 메일 발송 (rtgreen 로그인 전용):
        강등/탈퇴/승급 처리 후 대상자에게 자동 메일 안내
        다른 관리자 계정으로 로그인하면 메일 발송은 건너뜀
  · 장기미접속 조정: 6개월 이상 미접속, 6개월 주기
        매년 1/7월 1일 도래 (캘린더 기준)
      - 5레벨(준회원)    → 탈퇴
      - 6레벨(일반회원)  → 준회원
      - 7레벨(우수회원)  → 일반회원
      - 8레벨(최우수회원) → 우수회원
      - 9레벨(명예회원)  → 제외
  · 자동 트리거된 조정도 반드시 미리보기 → 사용자 확인 후 적용됩니다.
  · 회원 개별 등급 변경 (Ctrl+F 검색 → Ctrl+G 또는 더블클릭):
        선택한 회원의 등급을 5~9 중 어느 등급으로든 직접 변경 가능.
        본인 계정은 안전을 위해 이 화면에서 변경 불가.
"""


class HelpDialog(wx.Dialog):
    def __init__(self, parent):
        super().__init__(
            parent,
            title="단축키 안내",
            style=wx.DEFAULT_DIALOG_STYLE | wx.RESIZE_BORDER,
        )

        panel = wx.Panel(self)
        sizer = wx.BoxSizer(wx.VERTICAL)

        label = wx.StaticText(panel, label="단축키 및 사용법(&S):")
        sizer.Add(label, 0, wx.ALL, 8)

        self.text = wx.TextCtrl(
            panel,
            value=SHORTCUTS_TEXT,
            style=wx.TE_READONLY | wx.TE_MULTILINE | wx.TE_DONTWRAP,
            name="단축키 안내",
        )
        sizer.Add(self.text, 1, wx.EXPAND | wx.LEFT | wx.RIGHT, 8)

        close_btn = wx.Button(panel, wx.ID_OK, "닫기(&C)")
        sizer.Add(close_btn, 0, wx.ALIGN_CENTER | wx.ALL, 10)
        close_btn.SetDefault()

        panel.SetSizer(sizer)
        self.SetMinSize(wx.Size(600, 500))
        self.Fit()
        self.Centre()


def show_about(parent) -> None:
    info = wx.adv.AboutDialogInfo()
    info.SetName(APP_NAME)
    info.SetVersion(APP_VERSION)
    info.SetDescription(
        "초록등대 동호회 회원관리 자동화 도구.\n"
        "3개월마다 우수회원 백업, 6개월마다 장기미접속 회원 등급 조정.\n"
        "동호회관리자 권한이 있는 사용자만 사용할 수 있습니다."
    )
    wx.adv.AboutBox(info, parent)


# wx.adv 의존성 별도 import
import wx.adv  # noqa: E402
